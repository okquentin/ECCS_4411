1. Create a function (and program) that averages the numbers 3.5, 2.75, and 3.0

2. Write a simple program to ask the user for their name and display a Hello + the name they entered

3. Write a function that calculates the area of a rectangle it should have two parameters (length and width) and return the area. The program should take two integers from the user and call the function

4. Write a simple program that demonstrates Rust as useful

5. Take a string from the user and display it reversed

6. Sort the following array: [42, 16, 9, 24, 99, 7]